import React from 'react';
import { Outlet } from "react-router-dom";
const Login = () => {
    return (
        <div>
            <h1>hello ami login</h1>
        </div>
    );
};

export default Login;